export default function LoginAdmin() {
  if ((isadmin = true)) {
    return console.log(
      `hello ${name} you can proceide (send user to Adimn page)`
    );
  } else {
    return (
      <>
        <p>make a Standard massage Component and add it here</p>
      </>
    );
  }
}
