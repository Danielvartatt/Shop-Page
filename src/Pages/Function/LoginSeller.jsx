import user from "../Data/User";

export default function LoginSeller() {
  if ((seller = true)) {
    return console.log(`welcome seller : ${name}`);
  } else {
    return (
      <>
        <p>need a component here</p>
      </>
    );
  }
}
